//Demanar un text i un numero a l'usuari i eliminar el caracter de la paraula corresponent a la posicio del numero demanat
/*
const text = prompt("Introdueix un text:");
const numero = parseInt(prompt("Introdueix un número:"));

// Assegura que el número està dins dels límits del text
if (numero >= 0 && numero < text.length) {
    const textSenseCaracter = text.slice(0, numero) + text.slice(numero + 1);
    
    console.log("El text sense el caràcter en la posició " + numero + " és: " + textSenseCaracter);
} else {
    console.log("El número està fora dels límits del text.");
}

*/
